import { spacing } from "../../design";

export const REORDER_OVERLAP_THRESHOLD = 0.4;
export const FALLBACK_ROW_HEIGHT = 128;
export const TAB_BAR_CLEARANCE = spacing["5xl"] + spacing.lg;
export const TAB_BAR_SCROLL_INDICATOR_INSETS = { bottom: TAB_BAR_CLEARANCE };
